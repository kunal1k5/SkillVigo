/**
 * Utility: Standard Response Generator
 * 
 * Usage:
 * return res.json(generateResponse(200, 'Success', data));
 */

// export const generateResponse = (code, message, data = null) => ({
//   code,
//   message,
//   data,
//   timestamp: new Date().toISOString(),
// });
